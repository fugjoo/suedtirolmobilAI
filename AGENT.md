This repository uses the following contribution guidelines:

* Use 4-space indentation and keep code Python 3.8 compatible.
* Document public functions with docstrings.
* Run `pytest -q` before committing.
