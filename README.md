# suedtirolmobilAI

This project relies on a few Python packages used throughout the codebase.

## Dependencies

- openai
- requests

## Installation

Run the following command to install the required packages:

```bash
pip install -r requirements.txt
```
